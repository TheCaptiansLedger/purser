// Placeholder — the real Jobs tab (scrollable job table + detail modal)
// is built in #606/#607/#608. This stub exists only so /settings/jobs has
// something to route to while the shell (#610) is proven out.
export function JobsTab() {
  return <p className="text-body text-text-secondary">Jobs — coming soon.</p>
}
