// Placeholder — the real Config tab (cards per category, editable
// unlocked keys, locked keys read-only with a source badge, masked
// secrets) is built in #604. This stub exists only so /settings/config
// has something to route to while the shell (#610) is proven out.
export function ConfigTab() {
  return <p className="text-body text-text-secondary">Config settings — coming soon.</p>
}
