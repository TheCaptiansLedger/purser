// Placeholder — the real Cache tab (hit/miss stats, flush) is built in
// #614/#615/#616/#617. This stub exists only so /settings/cache has
// something to route to while the shell (#610) is proven out.
export function CacheTab() {
  return <p className="text-body text-text-secondary">Cache — coming soon.</p>
}
