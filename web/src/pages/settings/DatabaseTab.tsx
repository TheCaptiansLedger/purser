// Placeholder — the real Database tab (info, backup/restore) is built in
// #612/#613. This stub exists only so /settings/database has something
// to route to while the shell (#610) is proven out.
export function DatabaseTab() {
  return <p className="text-body text-text-secondary">Database — coming soon.</p>
}
