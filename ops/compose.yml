name: purser

volumes:
  purser-data:      # BadgerDB data directory — wipe to reset all library data
  purser-media:     # Cover art, logos, headshots downloaded by Purser
  # purser-content replaced by bind mount below — drop files into test-data/ to exercise the scanner

services:
  # ── Main application ─────────────────────────────────────────────────────────
  app:
    build:
      context: ..
      dockerfile: ops/Containerfile
    command: ["--config", "/config/purser.yaml"]
    ports:
      - "7474:7474"
    volumes:
      - ./purser.yaml:/config/purser.yaml:ro
      - purser-data:/data
      - purser-media:/media/purser
      - ../test-data:/media/content
    env_file:
      - path: ../.env
        required: false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:7474/api/v1/tags"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
