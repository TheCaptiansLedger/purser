# node_modules are never sent — they are OS-specific and rebuilt inside the
# container; sending them would corrupt the Linux build.
web/node_modules/

# Go build artifacts
purser
*.test

# Runtime / dev noise
.env
*.log
